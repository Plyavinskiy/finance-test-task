import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  paper: {
    padding: "0 20px 0 20px",
  },

  tableHeaders: {
    fontSize: "inherit",
    fontWeight: "bold",
  },
});
